---
title: "Programación de *gameplay* (III): Control de personajes y armas"
---

{{< pagebreak >}}

# Introducción

Una vez hemos visto elementos básicos de *gameplay* y como se usan ---como las colisiones, la detección de la ubicación de objetos en el espacio y la gestión de la entrada del jugador--- se pueden combinar para desarrollar aspectos más complejos, comunes a muchos tipos de juegos.

# Control de personajes

El controlador de personaje es el componente que se encarga del control del movimiento y de las acciones de los personajes.
La entrada del controlador son las acciones de movimiento indicadas por el jugador o por la IA ---por ejemplo, «muévete hacia adelante a 3 m/s», «salta» o «camina agachado»--- y la salida es la nueva posición del personaje, ajustada a las restricciones del mundo.

Por tanto, es responsabilidad del controlador del personaje hacer que el personaje se mueva sin atravesar muros o suelos, que salte y caiga, que pueda subir y bajar pendientes y escaleras o escalar muros.

El controlador de personaje suele tener características específicas del juego y del tipo de personaje que representa al jugador.
No es lo mismo manejar un vehículo que un personaje humanoide.
Respecto a estos últimos, en algunos juegos pueden saltar, nadar, trepar, correr o hacer «parkour», mientras que en otros no.

## Cápsula del personaje

Como hemos comentado anteriormente, la forma del personaje se modela con un volumen sencillo que se usa para detectar sus colisiones con el entorno.
Frecuentemente, este volumen es una cápsula, pero también puede ser una esfera o una caja, según se ajuste mejor a la figura del personaje y al movimiento.

![Actor **Character** de Unreal Engine.](figures/ue_empty_character.png)

Por lo general, es preferible usar cápsulas o esferas a las cajas, por varios motivos:

* Las capsulas y esferas presentan un único punto de colisión contra el plano del suelo o de una pendiente.
Asi es más sencillo implementar un controlador que ubique al personaje adecuadamente.

<!-- TODO: Incluir una figura -->

* La rotación de cápsulas y esferas en el eje Z no cambia la forma de estas al proyectarlas en el plano XY.
Esto significa que se puede rotar el personaje para que mire en una determinada dirección, sin que el controlador tenga que preocuparse por posibles colisiones.

    En muchos motores no se hace detección de la colisión al rotar un objeto.
    En ese caso, si se usa un cubo como colisión del personaje, el personaje podría penetrar en otros objetos si que la condición pueda ser detectada por el controlador.
    Entonces el personaje queda atrapado y ya no puede moverse por la escena.

<!-- TODO: Incluir una figura -->

## Controladores cinemáticos

Los controladores de personaje pueden ser **cinemáticos** o **dinámicos**.

Los **controladores de personaje cinemáticos** son los más comunes.
Ofrecen un control muy directo y estable y la disponibilidad general de motores de físicas, que necesitan los controladores dinámicos, es algo relativamente reciente.

En cada frame, los **controladores cinemáticos**: 

1. Calculan la posición final del personaje a partir de la entrada del controlador.
La entrada del controlador puede ser directamente el desplazamiento deseado ---control de primer orden--- pero, generalmente, suele ser la velocidad o la aceleración deseada para el personaje.
En estos casos, se suele usar las ecuaciones de la mecánica cinemática para calcular la posición final estimada para el personaje.

2. Intenta mover al personaje hasta dicha ubicación, detectando las colisiones.

3. En caso de colisión, se busca la posición viable ---sin superposiciones--- más cercana a la posición deseada y, finalmente, el personaje es movido hasta ese punto.

A este algoritmo se lo denomina ***collide and slide***, porque es frecuente que se implemente haciendo que si el personaje colisiona con otro objeto, el resto del desplazamiento lo haga deslizando el personaje paralelamente a la superficie del objeto.

<!-- TODO: Incluir una figura -->

El controlador suele soportar distintos métodos para hacer el *slide* y mover el personaje hasta una ubicación viable, según las mecánicas del juego: volar, caminar o correr, subir o bajar escalones o rampas, saltar y maniobrar en el aire, moverse sobre plataformas ---como ascensores o cintas transportadoras--- empujar objetos, nadar o escalar.

Estos métodos se utilizan según el estado en el que está personaje ---si está «saltando», porque el jugador ha pedido que salte, o está «nadando» porque ha entrado en un volumen marcado como de agua--- o según las colisiones detectadas ---si se detecta que está sobre el suelo, se puede caminar y superar algunos obstáculos pequeños, pero en el aire solo se puede caer---.

### Máquinas de estados

Cuando el controlador es complejo, las máquinas de estados son un buen patrón para su implementación.

De hecho, es frecuente que en el control de los personajes se usen al menos dos máquinas de estados que, por motivos obvios, van a presentar importantes similitudes.
Una para los modos de movimiento y otra para el control de la animación, según el estado del personaje.

<!-- TODO: Incluir una figura -->

En algunos casos puede ser conveniente utilizar otras máquinas de estados.
Por ejemplo, si en el juego tiene mucho peso el combate cuerpo a cuerpo, este suele seguir unas reglas que pueden implementarse fácilmente en su propia máquina de estados.
También puede usarse una máquina de estado para el cerebro de los NPC, estableciendo las reglas de comportamiento a alto nivel.

### Esquema general de un controlador cinemático

// TODO: Pendiente de escribir y revisar.

## Controladores dinámicos

Los **controladores de personaje dinámicos** aprovechan las funcionalidades del motor de físicas, por lo que en teoría son muy sencillos de implementar.

La idea es activar la simulación física sobre el personaje ---por ejemplo, añadiendo un componente **RigidBody** en Unity--- de forma que su movimiento y posición no se controla directamente, sino que está bajo el control del motor de físicas.
Este estima el movimiento considerando las colisiones y las fuerzas aplicadas sobre el objeto del personaje.

Desde el punto de vista de la entrada del jugador, esta se puede interpretar como la velocidad deseada ---control de segundo orden--- pero lo más frecuente es interpretarla como la aceleración ---control de tercer orden--- puesto que modificar directamente la velocidad de los objetos bajo simulación física, puede dar lugar a comportamientos no realistas.

En cada *tick* el controlador de personaje aplica una fuerza calculada a partir de la aceleración indicada como entrada del jugador.
Entonces el motor de físicas se hace cargo de calcular la posición futura del personaje automáticamente.

Sin embargo, el movimiento no suele ser tan suave y controlable como con un controlador **cinemático**.
Algunos problemas típicos de los controladores **dinámicos** son:

* **Carecen de control directo del desplazamiento**.
Los objetos con simulación física generalmente se controlan mediante impulsos y fuerzas.
No se los mueve directamente a la posición final deseada, sino que se tienen que calcular las fuerzas a aplicar ---a partir del desplazamiento deseado para el personaje--- y aplicarlas, con la esperanza de que la simulación física lleve al objeto hasta dicha posición deseada.
Obviamente, no resulta sencillo y no siempre funciona como esperamos.

* **Problemas con la fricción**.
Un personaje no debe deslizarse hacia abajo cuando está de pie sobre una rampa, lo que físicamente implica que la fricción debe ser muy alta.
Pero cuando sube por ella, esperamos que la fricción sea 0, de forma que no se mueva más despacio que por otras superficies del juego.
Tampoco esperamos que el personaje se mueva más despacio cuando se desliza contra un muro.

    Este tipo de comportamientos poco realistas son muy complicado de controlar ajustando los parámetros del motor de físicas.

* **Problemas con las restituciones**.
Cuando dos objetos chocan, uno puede superponerse temporalmente al otro.
Se denomina restitución al proceso por el cuál se resuelve la colisión para deshacer esa superposición.

    Esto puede crear cierto efecto rebote, porque el motor de físicas resuelve el cumplimiento de estas restricciones al movimiento, aplicando fuerzas lo suficientemente intensas como para separar lo más rápido posible los objetos superpuestos.
    Si las fuerzas son excesivas, los objetos pueden separarse demasiado, como si hubieran rebotado.

    El problema es que cuando un personaje se mueve muy rápido y colisiona contra una superficie ---quizás porque está corriendo o porque está cayendo desde cierta altura--- no queremos que rebote.

* **Saltos no deseados**.
Si un personaje corre por una rampa a gran velocidad, el motor de físicas generalmente lo hace saltar cuando llega al final de la rampa, debido a la inercia del movimiento, que le hace mantener la dirección temporalmente.
Sin embargo, no se suele desear ese efecto ---a menos que estemos desarrollando un simulador de conducción--- sino que esperamos que el personaje permanezca pegado al suelo en todo momento.

* **Rotaciones no desadas**.
Los personajes suelen permanecer de pie, por lo que no solemos querer que la cápsula que los rodea rote. Debe estar siempre en vertical, apoyada sobre uno de sus extremos en el suelo.
Lamentablemente, los motores de físicas suelen carecer de las características necesarias para restringir este tipo de rotaciones.

Algunas de estos problemas se pueden resolver usando *uniones*, que a veces no funcionan del todo bien porque son dinámicas.
Es decir, se basan en aplicar fuerzas para intentar restringir ciertos movimientos, lo que puede causar rebotes.

Otras opciones son que el controlador aplique por sí mismo ciertas fuerzas en momentos concretos o ajustar de forma cuidados algunos parámetros del motor de físicas.

Todo para emular con un motor de físicas un comportamiento mucho más simple de aquel para el que el motor fue diseñado.
Por eso, suele ser mucho más sencillo y estable implementar directamente el comportamiento deseado en un controlador cinemático, usando el motor de físicas para otros aspectos del juego.

## Implementaciones en motores de videojuegos

### Unity

En Unity el **CharacterController** es un controlador **cinemático** bastante simple, por lo que muchas veces acaba teniendo que ser sustituido por una implementación propia o comprada en la *Asset Store*.

Básicamente, implementan el algoritmo básico de ***collide and slide***, lo que permite mover el personaje sobre una superficie, con pendientes y escalones, detectando colisiones.

El controlador permite obtener información sobre la colisiones, así como saber si el personaje está sobre el suelo.
Con esta información, podemos mejorar el controlador implementado otros tipos de movimiento, como: saltar, agacharse, *correr en la pared*, subir escaleras o nadar.

### Unreal Engine

En Unreal Engine el **Character** incluye el componente **CharacterMovementComponent**, que se encarga del control del movimiento del personaje y también se implementa de forma **cinemática**.

**CharacterMovementComponent** ofrece todo el soporte necesario para un *shooter* en primera o tercera persona, con modos para caminar, saltar, agacharse, volar o nadar; pudiendo ajustar distintos parámetros en cada modo ---como la velocidad máxima del movimiento o las pendientes y alturas de escalones máximas---. 
Para añadir otros modos ---como escalar o subir escaleras de mano--- solo es necesario heredar de la clase e implementar un *modo de movimiento personalizado*.

**CharacterMovementComponent** y el actor **Character** están bien integrados, por lo que también se encarga de hacer ajustes al resto de componentes del **Character**, según el modo de movimiento.
Por ejemplo, ajustar el tamaño de la cápsula cuando el personaje se agacha.

#### Modo de movimiento personalizado

El método `SetMovementMode()` de **CharacterMovementComponent** permite conmutar entre los diferentes modos de movimiento.
Uno de estos modos es el **Custom**, que admite sub-modos indicando un entero diferente para identificarlo en el segundo argumento de `SetMovementMode()`, como se puede observar en la @fig-set-movement-mode.

![Set Movement Mode.](figures/set-movement-mode.png){#fig-set-movement-mode}

Para implementar los *modos de movimiento personalizados*[@UnrealCharacterMovementComponent], se puede heredar de **CharacterMovementComponent** en C++ y sobrescribir `PhysCustom`, que es el método invocado cuando el modo de movimiento es **Custom**.

Una de las tareas que realizar el `PhysCustom` por defecto, es llamar al evento **UpdateCustomMovement** del **Character**.
Como el **CharacterMovementComponent** no es directamente heredable en Blueprint, este evento sería el lugar para implementar movimientos personalizados en Blueprint.

#### Integración el AIController

El **CharacterMovementComponent** se integra con el sistema de navegación, permitiendo que el **AIController** de un NPC marque un destino y el sistema de navegación considere para llegar a él los modos de movimiento soportados por el **CharacterMovementComponent**.
Con la ruta obtenida, el **AIController** mueve el personaje dando órdenes de desplazamiento al **CharacterMovementComponent**, para llegar a los distintos puntos de dicha ruta usando los modos de movimiento adecuados.

Como se puede observar en la @fig-ue-character-movement-hierarchy, **CharacterMovementComponent** hereda de **NavMovementComponent**, que ofrece los métodos:

```{mermaid}
%%| label: fig-ue-character-movement-hierarchy
%%| file: figures/ue-character-movement-hierarchy.mm
%%| fig-width: 15cm
%%| fig-cap: Diagrama de clases involucradas en el control de NPC.
```

* `AddInputVector()`, para recibir la entrada del jugador.
El método `AddMovementInput()` de los actores **Pawn** y **Character** llama al `AddInputVector()` de su **MovementComponent** para comunicar al componente le entrada del jugador.

* `RequestDirectMove()`, sirve para que el **AIController** indique la dirección y velocidad deseada para el movimiento del personaje.

* `RequestPathMove()`, sirve para que el **AIController** indique solo la dirección del movimiento.
En general, se interpreta como la dirección en la que se debe acelerar, escalada entre 0.0 y 1.0, de forma que si el vector tiene tamaño 1.0, es que el **AIController** desea que el movimiento ocurra con la máxima aceleración.
Al aproximarnos al destino, el controlador puede ir decreciendo el tamaño del vector para ir desacelerando.

Para indicar al **AIController** si debe usar `RequestDirectMove()` o `RequestPathMove()`, se puede usar la propiedad **UseAccelerationForPaths** del **MovementComponent**.
Si vale `true`, el **AIController** llamará a `RequestPathMove()`.

Además, **NavMovementComponent** tiene la propiedad **NavAgentProps** para indicar información sobre el agente ---como los modos de movimiento que soporta o su tamaño--- que el subsistema de navegación usa para establecer rutas adecuadas por el nivel.

# Puertas, plataformas y ascensores

// TODO

<!---
# Plataformas

¿Código en el plataforma o en el personaje?


# Diseño dirigido por datos

# Programación dirigida por eventos


# Timers

# Damage API
--->

# Armas

<!---

# Armas

# *Hitboxes*

**hutboxes**

--->

## Balas y proyectiles

Para implementar balas y otro tipo de proyectiles en juegos de acción, básicamente podemos usar *raycasts*, proyectiles balísticos o combinar de ambas opciones.

### Ray castings

El uso de *raycasts* es, con diferencia, la opción más sencilla.
Al disparar, se lanza un rayo para descubrir si algún objeto es alcanzado en la trayectoria.
Si es así, se le aplica dañó.

El *raycast* puede limitarse a una distancia máxima, si el arma tiene un alcance limitado.
También se pueden detectar múltiples objetos en la trayectoria, simulando que el arma tiene una gran capacidad de penetración.
O se pueden marcar ciertas superficies como reflectivas, de forma que si impacta en una de ellas hagamos un nuevo *raycast* en la dirección de reflexión, simulando que los disparos pueden rebotar.

La ventaja es que los *raycasts* son una operación muy eficiente, que no necesita simulación física ni el cálculo de ecuaciones de mecánica cinemática.
Además, es muy sencilla de utilizar en juegos multijugador en red, porque el *raycasts* se hace en el servidor, solo teniendo que comunicar a los clientes el daño infligido y los efectos visuales.

Como contrapartida, el impacto es instantáneo, por lo que no hay manera de esquivar las balas.
Tampoco resulta realista en disparos a muy larga distancia, donde la gravedad, el viento y otros factores ambientales deberían tener efecto en la trayectoria del proyectil.
Por tanto, el uso de *raycasts* no es la mejor opción si interesa crear una experiencia realista.

### Proyectiles balísticos

La forma de conseguir una experiencia más realista e inmersiva es lanzar proyectiles.
Es decir, crear objetos, impulsarlos en la dirección del disparo y simular su movimiento según su masa, velocidad, gravedad, dirección del viento o cualquier otra fuerza que pueda alterar la dirección del proyectil.

Esta solución permite que los proyectiles se puedan esquivar ---al menos en disparos a grandes distancias o cuando se introducen efectos como el *bullet-time*, donde el tiempo se ralentiza temporalmente---.
También obliga al jugador a prever el movimiento de los enemigos y del proyectil, para disparar allí donde cree que estará cuando el proyectil llegue.
Y permite añadir tipos diferentes de armas que difícilmente se pueden implementar mediante *raycasts*, como granadas y lanzacohetes.

Como contrapartidas:

* El uso de proyectiles es bastante más costoso que los *raycasts*, porque crear decenas de objetos y simular su movimiento, no resulta barato.
En este sentido, es necesario considerar el uso de *pools* de objetos para reusar los objetos creados, reduciendo el coste de crearlos en el momento.

* Los cálculos del movimiento presentan varios retos:

// TODO: Explicar esto mejor

  * Uno de ellos es hacer los cálculos con la suficiente frecuencia, porque si el proyectil se mueve muy rápido ---cubriendo grandes distancia entre intervalos de tiempo--- se puede dar el caso de que no se detecten algunas colisiones.
  Por eso es común que los cálculos se hagan con mayor frecuencia y de forma independiente a la tasa de refresco del *render*.

  * En cada paso de simulación la trayectoria suele aproximarse mediante un segmento rectilíneo.
  Para calcular una trayectoria más precisa, en cada paso de simulación se puede usar cierto número de subpasos rectilíneos

  <!--- TODO: Imagen ---> 

  * Con el objeto de evitar fallar en la detección de posibles obstáculos, puede ser necesario considerar algunas de las técnicas de detección continua de colisiones[@TorresD3DFisicas], como añadir a los proyectiles una cola invisible, hacer un *raycast* o traslación con *barrido* con la forma del proyectil en cada paso de simulación.

* El uso de proyectiles suponen un reto adicional en los juegos multijugador en red, porque implica tener decenas de objetos cuyo movimiento debe sincronizarse entre todos los clientes, para evitar crear inconsistencias en la experiencia de los distintos jugadores.

    Por lo general, cada cliente ejecuta su propia simulación de los proyectiles, que se va corrigiendo periódicamente con la información que llega del servidor, que ejecuta su propia simulación y que es la única fuente de información confiable.

### Sistemas híbridos {#_sistemas_híbridos}

En muchos videojuegos se utilizan ambos tipos de soluciones.
Esto da la opción de tener una gran cantidad de armas con diferentes características, de forma que en cada una se usa la solución que ofrece los mejores resultados.

# Damage API

// TODO: Hablar del Damage API de Unreal Engine

# Referencias {.unnumbered}

::: {#refs}
:::