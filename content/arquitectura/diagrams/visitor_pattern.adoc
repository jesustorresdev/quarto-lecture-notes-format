[plantuml, format="svg"]
....
hide empty members
interface IDamageable {
    +Health : float
    +Defense: float
    +Resistance : float
}

IDamageable <|-- Wizard
IDamageable <|-- Knight
IDamageable <|-- Archer

interface IVisitor
{
    +ApplyDamage(damageable: IDamageable)
}

class Bow {
    +ApplyDamage(wizard: Wizard)
    +ApplyDamage(knight: Knight)
    +ApplyDamage(archer: Archer)
}

class Axe {
    +ApplyDamage(wizard: Wizard)
    +ApplyDamage(knight: Knight)
    +ApplyDamage(archer: Archer)
}

class Spell {
    +ApplyDamage(wizard: Wizard)
    +ApplyDamage(knight: Knight)
    +ApplyDamage(archer: Archer)
}

IVisitor <|-- Bow
IVisitor <|-- Axe
IVisitor <|-- Spell
....